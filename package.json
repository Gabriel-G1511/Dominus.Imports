{
  "name": "dominus-imports",
  "version": "1.0.0",
  "description": "Site institucional da Dominus Imports",
  "scripts": {
    "build": "grunt less && grunt cssmin && mkdir -p dist/css dist/src && cp index.html dist/index.html && cp -r src/img dist/src/img && cp dist/css/main.min.css dist/css/main.min.css",
    "start": "grunt watch"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "less": "^4.4.2",
    "minify": "^14.1.0"
  },
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-contrib-cssmin": "^5.0.0",
    "grunt-contrib-less": "^3.0.0",
    "grunt-contrib-watch": "^1.1.0"
  }
}